services:
  api:
    # Keep the benchmark server open longer than Nginx's 5-second upstream cache.
    command: ["uvicorn", "ticketing.api:app", "--host", "0.0.0.0", "--port", "8000", "--limit-concurrency", "256", "--timeout-keep-alive", "10"]
    environment:
      DB_POOL_WAIT_MS: '500'
